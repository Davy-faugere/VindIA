# Dépendances runtime (NON requises pour les tests unitaires stdlib / la CI).
# Installées sur le VPS au câblage live du périmètre B.
PyMySQL>=1.1

# Runtime live (installés sur le VPS / dans un venv ; cf. docs) :
# mistralai>=1.5,<2   # LLM (chat) + STT (audio.transcriptions). <2 : 2.4.x cassé.
# livekit             # client temps réel
# livekit-api         # mint de tokens
# piper-tts           # TTS souverain FR self-host (voix fr_FR-siwis-medium)
